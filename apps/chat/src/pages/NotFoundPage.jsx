/**
 * NotFoundPage
 * Page affichée pour toute route inconnue (404).
 */
export default function NotFoundPage() {
  return (
    <div className="h-screen flex items-center justify-center">
      {/* TODO: message 404 + lien retour */}
    </div>
  )
}
