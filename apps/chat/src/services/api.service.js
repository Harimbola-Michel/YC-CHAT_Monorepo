/**
 * api.service
 * Client HTTP de base (instance axios/fetch configurée) pour les appels REST.
 */

// TODO: configurer l'URL de base, les headers, les intercepteurs
export const apiClient = {
  // get, post, put, delete...
}
