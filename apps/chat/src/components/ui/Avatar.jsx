/**
 * Avatar
 * Photo/initiales d'un utilisateur, avec indicateur de statut optionnel.
 */
export default function Avatar() {
  return (
    <div className="w-10 h-10 rounded-full bg-gray-200 flex items-center justify-center shrink-0">
      {/* TODO: image ou initiales, pastille de statut (en ligne/hors ligne) */}
    </div>
  )
}
